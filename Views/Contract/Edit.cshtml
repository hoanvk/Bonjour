<div class="modal fade" id="editContractModal" tabindex="-1" aria-labelledby="editContractModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="editContractModalLabel">Edit Contract</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form class="row g-3" asp-action="Edit" method="post" id="editContractForm">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" value="" />
                    <div class="col-md-12">
                        <label for="Name" class="form-label">Name</label>
                        <input type="text" class="form-control" id="Name" name="Name" placeholder="Hợp đồng">
                        <div class="invalid-feedback"></div>
                    </div>
                    <div class="col-md-12">
                        <label for="Customer" class="form-label">Customer</label>
                        <input type="text" class="form-control" id="Customer" name="Customer"
                            placeholder="Tên khách hàng">
                        <div class="invalid-feedback"></div>
                    </div>
                    <div class="col-md-6">
                        <label for="StartDate" class="form-label">Start Date</label>
                        <input type="text" class="form-control date" id="StartDate" name="StartDate">
                        <div class="invalid-feedback"></div>
                    </div>
                    <div class="col-md-6">
                        <label for="EndDate" class="form-label">End Date</label>
                        <input type="text" class="form-control date" id="EndDate" name="EndDate">
                        <div class="invalid-feedback"></div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" onclick="Contract.editContractHandler()">Save
                    changes</button>
            </div>
        </div>
    </div>
</div>